= render "header"

.card-main
  .card-container
    .card-container__title
      クレジットカード情報変更
    .card-container__label
      = form_tag(card_path, method: :put, action: "create", id: 'charge-form',  name: "inputForm") do
        = render partial: "form"
        #card_token
          = submit_tag "変更する", id: "token_submit", class: "top__margin"

= render "footer"